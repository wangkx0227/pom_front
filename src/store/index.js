import Vue from "vue";
import Vuex from "vuex";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    userToken: null, // 用户token
  },
  getters: {},
  mutations: {},
  actions: {},
  modules: {},
});
